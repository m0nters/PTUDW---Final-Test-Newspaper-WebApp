<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= article.title %></title>
    <link href="/css/output.css" rel="stylesheet" />
  </head>

  <body class="bg-gray-100">
    <div class="container mx-auto px-4 py-8 max-w-4xl">
      <!-- Title Section -->
      <h1 class="text-4xl font-bold text-gray-900 mb-4">
        <%= article.title %>
      </h1>

      <!-- Premium Tag, Author & Category -->
      <div class="flex items-center gap-4 mb-6">
        <% if (article.is_premium) { %>
        <span
          class="bg-gradient-to-r from-purple-600 to-blue-500 text-white px-3 py-1 rounded-full text-sm font-semibold"
        >
          Premium
        </span>
        <% } else { %>
        <span
          class="bg-gray-600 text-white px-3 py-1 rounded-full text-sm font-semibold"
        >
          Standard
        </span>
        <% } %>
        <span class="text-gray-600">By <%= article.author %></span>
        <span class="text-gray-600 text-3xl">•</span>
        <span class="text-gray-600"
          >Category: <%= article.category_name %></span
        >
      </div>

      <!-- Quick View Section -->
      <div class="bg-white p-6 rounded-lg shadow-md mb-8">
        <h2 class="text-xl font-semibold mb-3">Xem nhanh</h2>
        <p class="text-gray-600"><%= article.abstract %></p>
      </div>

      <!-- Image -->
      <% if (article.img_url) { %>
      <div class="mb-8">
        <img
          src="<%= article.img_url %>"
          alt="<%= article.title %>"
          class="w-full h-full object-cover rounded-lg shadow-lg"
        />
      </div>
      <% } %>

      <!-- Content -->
      <div class="bg-white p-8 rounded-lg shadow-md mb-8">
        <div class="prose max-w-none"><%- article.content %></div>
      </div>

      <!-- Back Button -->
      <div class="flex text-center justify-start">
        <a
          href="/articles"
          class="inline-block bg-slate-800 text-white px-6 py-2 rounded-lg hover:bg-slate-700 transition-colors"
        >
          ◄ Back to Articles List
        </a>
      </div>
    </div>
  </body>
</html>
